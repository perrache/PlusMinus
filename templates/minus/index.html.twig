{% extends 'base.html.twig' %}

{% block title %}Minus index{% endblock %}

{% block body %}
    <h1>Minus index</h1>
    <a class="{{ rawA|raw }}" href="{{ path('app_minus_new') }}">Create new</a>

    <table class="{{ rawTable|raw }}">
        <thead>
        <tr>
            <th>Id</th>
            <th>Value</th>
            <th>Dat</th>
            <th>Comment</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for minu in minuses %}
            <tr>
                <td>{{ minu.id }}</td>
                <td>{{ minu.value }}</td>
                <td>{{ minu.dat ? minu.dat|date('d-m-Y') : '' }}</td>
                <td>{{ minu.comment }}</td>
                <td>
                    <a class="{{ rawA|raw }}" href="{{ path('app_minus_show', {'id': minu.id}) }}">show</a>
                    <a class="{{ rawA|raw }}" href="{{ path('app_minus_edit', {'id': minu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="{{ rawA|raw }}" href="{{ path('app_minus_new') }}">Create new</a>
{% endblock %}
